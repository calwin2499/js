"use strict" /* Treat all the JS code as a new version. 
Even if the code is of previous version using this command will make use of the new version
and u can use the new version of code as a new developer in a old code base*/

// alert(3+3)   //we are using nodejs instead of browser so it can't give the out put as 6! 

console.log(3
    +3)   //This is wrong the code should be readable 

//ecma script is the standard documantation and mdn is also another type of documantation

// number => 2 to power 53
// bigint
// string =>""
// boolean => true/false
// null => standalone value
// undefined =>
// symbol => unique

// object

console.log(typeof null);        //object
console.log(typeof undefined);   //undefined


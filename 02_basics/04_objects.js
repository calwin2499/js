/* There are two ways to declare the object and there are two example below */
const tinderUser = new Object() // this is a singleton object 
const tinderUser = {}           // non-singleton object
console.log(tinderUser);
